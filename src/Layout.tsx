import React from 'react';
import Moviecard from './Course'

export const Layout = () => {
  return (
    <main className="wrap">
      This is Layout!

      <Moviecard />
    </main>
  )
}

export default Layout;